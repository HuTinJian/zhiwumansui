# 本地开发密钥（切勿提交到 Git）
.dev.vars
.dev.vars.*

# Wrangler / 依赖 / 构建产物
.wrangler/
node_modules/
dist/

# 本地工具产生的数据（dsh-graph 目标看板的脚手架），与网站无关，不要提交也不要部署
.dsh-graph/

# 改版时的本地验证脚手架（假数据预览页 + 截图），只是过程凭证，不进仓库也不部署
.verify/
# 预览页必须放根目录（相对路径才指得对），单独点掉
verify-*.html

# 编辑器与系统文件
.DS_Store
Thumbs.db
